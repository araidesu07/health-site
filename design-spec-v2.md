# 記事レイアウト改修 仕様書 v2（health-evidence.jp）

比較記事を「調査レポート」から「専門家による商品比較メディア」に変える。
既存の `design-spec.md`（配色・タイポグラフィ・表）は**そのまま有効**。
本仕様はその上に構成要素を追加するもの。

---

## 0. 設計方針

読者の動線を、**判断しやすい順**に並べ替える。

```
① 結論          ← 3秒で分かる
② タイプ別早見表  ← 10秒で自分の答えが出る
③ 商品カード      ← 30秒で見比べられる
④ 比較表         ← 数値を確認したい人向け
⑤ 選び方のポイント
⑥ 詳細な根拠・制度
⑦ 出典・確認していない範囲
```

既存記事の詳細分析は**削らない**。⑥以降に置く。

配色・余白・表のスタイルは `design-spec.md` に従う。
派手なECサイト風・煽りの強いランキング風にはしない。

---

## 1. 新規コンポーネント一覧

Astro コンポーネントとして実装し、Markdown から呼び出せるようにする。

| コンポーネント | 用途 |
|---|---|
| `ConclusionBox.astro` | 記事冒頭の結論ボックス |
| `TypeGuide.astro` | タイプ別おすすめ早見表 |
| `ProductCard.astro` | 商品カード（複数並べる） |
| `PointList.astro` | 「どう選ぶ？」の番号付きカード |
| `NoticeBox.astro` | 前提・注意事項のボックス |
| `Toc.astro` | 目次（サイドバー） |
| `AuthorBox.astro` | 著者プロフィール（サイドバー） |

---

## 2. ConclusionBox（結論ボックス）

記事タイトル・著者情報の直下に配置する。

### 見た目
- 背景 `--color-accent-light`、左に `--color-accent` の縦線（4px）
- 角丸 6px、パディング 1.5rem
- 見出し「この記事の結論」＋チェックアイコン
- 項目は箇条書き。各行の先頭に小さなチェックマーク

### 中身のルール
- 3〜5項目
- **製品名を「」で囲んで明示する**
- 各項目は1行に収める
- 「最も安い」等の断定を避け、
  **「〜で比べると〇〇が最も低い」**のように基準を明示する

### 記述例（Markdown内）
```
<ConclusionBox items={[
  "鉄1mgあたりのコストで比べると「DHC ヘム鉄 徳用90日分」が最も低い",
  "1日あたりのコストで比べると「ネイチャーメイド 鉄」が最も低い",
  "葉酸・ビタミンB12も含む製品なら「ファンケル 鉄&葉酸」",
  "単一成分の製品を選ぶなら「ネイチャーメイド 鉄」"
]} />
```

---

## 3. TypeGuide（タイプ別おすすめ早見表）

結論ボックスの直下。横並び4枚（スマホでは2列×2段）。

### 見た目
- カード幅は均等。背景は各カードで淡い色を変える
  （緑系 / 青系 / 紫系 / オレンジ系。彩度は低く）
- 上部にラベル（太字・カラー文字）
- その下に1行の説明（グレー・小さめ）
- 商品画像（80×80px程度）
- 製品名（2行まで）
- **キーとなる数値を大きく強調**（`--color-primary-dark`）

### 中身のルール
- 4枚固定ではなく、3〜4枚
- ラベルは**選び方の軸**を書く。読者の属性は書かない
  - ○「コスパ最重視」「鉄の量あたりで選びたい」「成分も一緒に摂りたい」
  - ✗「妊娠中の方」「貧血気味の方」「疲れやすい方」
- 表の直下に価格の前提を注記する

---

## 4. ProductCard（商品カード）

このデザインの中核。横並び4枚（スマホでは1列）。

### 構成（上から）
1. **特徴タグ**（カード上部・小さなピル型ラベル）
   例：「鉄1mgあたり最安」「成分バランスが良い」「1日あたり最安」
2. **商品画像**（正方形・160×160px程度）
   - 楽天の画像URLの `_ex=64x64` を `_ex=300x300` に変更して使用する
3. **製品名**（2〜3行。楽天のSEO用長文名は使わず、短い正式名称）
4. **主要数値**（鉄 / 葉酸 / B12 / 銅 の含有量。1日目安量あたり）
5. **おすすめポイント**（緑背景のボックス・箇条書き2〜3項目）
6. **読み・注意点**（赤系背景のボックス・箇条書き1〜2項目）
7. **CTAボタン**（楽天リンク。`rel="nofollow sponsored"` を付与）

### 「おすすめポイント」「読み・注意点」の書き方ルール

**これがこのサイトの差別化そのもの。** 以下を守る。

**書いてよいこと**
- 含有量・粒数・内容量・価格から導ける事実
- 原材料表示の粒度（化合物名まで書かれているか、原料名のみか）
- 成分の種類の多寡
- 継続コストの傾向

**書いてはいけないこと**
- 吸収・吸収率・利用効率に関する記述
- 疾病名・症状名（貧血、めまい、疲れ 等）
- 対象者（妊娠中、月経中 等）
- 「効く」「改善」「補える」等の効果を示す表現
- 根拠のない最上級（「最も優れた」「一番おすすめ」）

**表現の型**
```
事実 → その事実がどういう意味を持つか
```
例：
- ○「ヘム鉄の原料名のみの表示で、化合物名までは確認できない」
- ✗「ヘム鉄なので吸収されやすい」

### CTAボタン
- ラベル「楽天で価格を見る」
- 背景 `--color-primary`、白文字、角丸 6px
- **「今すぐ購入」「最安値はこちら」等の煽り文言は使わない**
- カード下部に固定配置（カードの高さを揃える）

---

## 5. 比較表の強化

既存の表スタイル（`design-spec.md` §5）を維持しつつ、以下を追加。

- **最小値のセルを `--color-warn`（オレンジ系）の太字**にする
  （「1日あたりのコスト」列と「鉄1mgあたりのコスト」列）
- 製品名の列は最小幅 12em を確保する
- 数値列は右寄せ・桁揃え（既存仕様のまま）
- 表の直下に価格の前提注記を必ず置く

---

## 6. PointList（どう選ぶ？比較のポイント）

横並び3枚。番号付き。

- 番号は丸背景（`--color-primary`・白文字）
- 見出し（太字）＋ 説明文（2〜3行）
- 背景 `--color-bg-subtle`、角丸

---

## 7. NoticeBox（注意事項）

記事末尾、CTAの前に配置。

- 背景 `--color-warn-light`、左に `--color-warn` の縦線
- アイコン（⚠️ 相当）
- 文言（固定）：
  > サプリメントは食品であり、効果効能を保証するものではありません。
  > 体調や持病、服薬中の薬がある場合は、医師・薬剤師にご相談ください。

---

## 8. サイドバー（PC のみ表示）

画面幅 1100px 以上で右カラムに表示。それ未満では非表示または本文下に回す。

### 8-1. 目次（Toc）
- H2 見出しから自動生成
- 番号付きリスト
- 現在位置をハイライト（スクロール連動）
- **スティッキー**（スクロールしても追従）

### 8-2. 著者ボックス（AuthorBox）
- 見出し「この記事の著者」
- アイコン画像（丸型・イラストで可。実写・実在人物の写真は使わない）
- 「トウノ（薬剤師）」
- 短い紹介文（2〜3行。CLAUDE.md §1 の固定情報から）
- 「運営者情報を見る」ボタン（`/about/` へ）

### 8-3. 前提・注意点
- 見出し「この記事の前提・注意点」
- 箇条書き3〜4項目
- 価格の前提、確認していない範囲など

### 8-4. 関連記事
- 見出し「関連する記事」
- 3件まで。サムネイル＋タイトル
- **記事が存在しない場合はセクションごと非表示にする**

### 8-5. 出典
- 見出し「情報の出典」
- 短い説明＋「出典一覧を見る」ボタン（記事内アンカー）

---

## 9. ヘッダーの強化

- 左：ロゴ＋サイト名「薬剤師の成分チェック」
  - サブタイトル「成分と根拠から、あなたに合う商品を見つかる」
    → **日本語が不自然なので「成分と根拠から選ぶ」等に修正すること**
- 中央〜右：グローバルナビ
  - サプリメント / オーラルケア / 運営者情報 / 編集ポリシー
- パンくずリスト（ヘッダー下）
  - ホーム > カテゴリ > 記事タイトル

---

## 10. フッター

既存のまま。リンク一覧＋コピーライト。

---

## 11. スマホ対応

| 要素 | スマホでの挙動 |
|---|---|
| サイドバー | 非表示（目次のみ本文冒頭に折りたたみで表示） |
| TypeGuide | 2列×2段 |
| ProductCard | 1列（縦積み） |
| 比較表 | 横スクロール（既存仕様） |
| PointList | 1列 |

**スマホでの ProductCard の見やすさが最優先。**
画像・製品名・主要数値・CTA が1画面に収まること。

---

## 12. アフィリエイトリンクの実装ルール

- すべてのアフィリエイトリンクに `rel="nofollow sponsored"` を付与
- `target="_blank"` で別タブ
- リンクURLは記事の frontmatter または別ファイルで管理し、
  本文にHTMLを直接埋め込まない
- **A8のHTMLタグをそのまま貼らない**（固定幅スタイルがデザインを壊すため）
  必要なのはリンクURLと画像URLのみ

### 計測用画像について
A8のリンクHTMLに含まれる 1×1px の計測用画像（`www○○.a8.net/0.gif`）は、
成果計測に必要な場合があるため、**A8の規約を確認したうえで扱いを決める**。

---

## 13. 実装順序

1. `design-spec.md` の既存スタイルは変更しない
2. コンポーネントを1つずつ作成し、都度ローカルで確認する
3. まず `ConclusionBox` `ProductCard` `TypeGuide` の3つ
4. 次にサイドバー
5. 最後にヘッダー・パンくず

**一度に全部作らない。** 1コンポーネントごとに表示確認する。

---

## 14. 確認項目

- [ ] スマホで商品カードが1画面に収まる
- [ ] 商品画像が正しく表示される（300×300で取得）
- [ ] CTAボタンに `rel="nofollow sponsored"` が付いている
- [ ] 結論ボックスに「最も安い」等の無条件の断定がない
- [ ] タイプ別ラベルに読者の属性（妊娠中 等）が入っていない
- [ ] 商品カードの記述に吸収・効果に関する表現がない
- [ ] 比較表の最小値が視覚的に強調されている
- [ ] 目次がスクロールに追従する（PC）
- [ ] 関連記事が存在しない場合、セクションが非表示になる
- [ ] 既存記事（フロス）のレイアウトが壊れていない
